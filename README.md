# Online Arena — Thieves on My Trail 🎮⚡

A grid-based web game where you must capture the “thieves” while avoiding getting caught.  
Modern **Full Stack TypeScript** project: **NestJS + GraphQL + Prisma + Postgres** on the backend, **React + Vite + Tailwind + Apollo** on the frontend.

---

## ✨ Features

- ✅ JWT authentication (Register / Login)
- ✅ Playable directly in the browser
- ✅ Game results persisted in DB (win/lose + score + timestamp)
- ✅ Player profile: stats + match history
- ✅ **Demo mode** available without an account (`/demo`)
- ✅ **Health endpoint** (`/healthz`, GET)
- ✅ Responsive UI built with Tailwind

---

## 🧱 Tech Stack

### Backend (`/api`)
- NestJS
- GraphQL (Apollo Server via Nest)
- Prisma ORM
- PostgreSQL (local via Docker / production via Neon)
- JWT Auth (Passport + JWT Strategy)

### Frontend (`/web`)
- React + TypeScript
- Vite
- Tailwind CSS
- Apollo Client (GraphQL)
- React Router

---

